# Copyleft (c) by Julia Samuseva 2024.
#--------------------------------------------------
name: My_GitHubActions_Basics

on:
  push:
    branches:
      - master

jobs:
  my_testing:
    runs-on: ubuntu-latest   
    steps: 
      - name: Print Hello Message
        run: echo "Hello World from Testing"
  
  my_deploy:
    runs-on: ubuntu-latest 
    steps: 
      - name: Print Hello Message
        run: echo "Hello World from Deploy job"
